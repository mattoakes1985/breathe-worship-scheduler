# [Moved] This filename is deprecated

This file previously held the PRD. It has been superseded to fix a real bug: this filename said "v1.0" while the document's actual content had moved to v1.2 (multi-agent execution plan, gamified stats, serving preferences, and more), which meant every reference to "the PRD" in `CLAUDE.md`, `MEMORY.md`, and `AGENTS.md` pointed at a filename that implied stale content even though the content itself was current.

**The PRD now lives at:** `Breathe-Worship-Scheduler-PRD.md` (no version number in the filename — version is tracked only in the document's own header table and its "Changes in vX.X" changelog, so this specific bug cannot recur on the next revision).

If you are an agent and you were told to read this file, read `Breathe-Worship-Scheduler-PRD.md` instead. If you find another reference to this old filename anywhere in this project, that reference is stale and should be corrected.
